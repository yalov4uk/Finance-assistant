spring:
  application:
    name: '@project.artifactId@'
  profiles:
    active: native
  cloud:
    config:
      server:
        git:
          uri: file://home/yalov4uk/projects/Finance-assistant-config
