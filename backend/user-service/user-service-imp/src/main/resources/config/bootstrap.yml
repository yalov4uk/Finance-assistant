spring:
  application:
    name: user-service
  cloud:
    config:
      discovery:
        enabled: true
        serviceId: config
      fail-fast: true

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka
