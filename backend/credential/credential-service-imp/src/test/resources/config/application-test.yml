spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:myDb;INIT=runscript from 'classpath:db/init.sql'

mybatis:
  typeAliasesPackage: com.perfect.team.credential.imp.model
  mapper-locations: classpath:mybatis/*.xml

stubrunner:
  idsToServiceIds:
    user-service-imp: user-service
